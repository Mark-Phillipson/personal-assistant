
Please can we reply to David asper his message in the screenshot And explain that the client setting is just currently a default and for it to take effect each project for that client would have to be set as well which would happen automatically when a project is created in the future

 also please state that if he wishes I can set up the ability to set certain projects to use the current client default for a specific date range of projects

  and I will make the changes to the report so you can hide or show the commission depending on the current users membership of admin role or not

Hi David — thanks for the screenshots.

The null report usually happens because the client-level sales rep is a default and doesn't retroactively set existing projects. You can assign the rep on those specific projects to fix the report immediately, or I can update the report logic to fall back to the client default when a project has no rep.

I can add a show/hide commission option in the report and restrict that control to users with the admin role so membership determines visibility.

I can also enable applying the client default to existing projects for a specified date range. If you'd like that, tell me which clients or the date range to target and I'll implement it.